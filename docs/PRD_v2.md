# 🌳 Project Requirement Document (PRD): Gratitude Grove

## 1. 프로젝트 개요 (Overview)
- **서비스명:** Gratitude Grove (감사의 정원)
- **핵심 가치:** 사용자가 하루 최대 3개의 감사한 일을 기록하고, 이를 '작은 숲'처럼 시각화하여 공유하는 힐링 커뮤니티.
- **디자인 컨셉:** 미니멀리즘(Google Style), 백색 배경의 깔끔함, 숲과 자연의 메타포(아이콘).

## 2. 기술 스택 (Tech Stack)
- **Frontend:** React, TypeScript, Tailwind CSS
- **Backend/DB:** **Supabase** (Auth, Database - PostgreSQL)
- **Icons:** **Lucide-react** (필수 사용)
- **Font:** 나눔 바른 고딕 (Nanum Barun Gothic) - Google Web Font 또는 CDN 활용
- **Deployment:** **Vercel**

## 3. UI/UX 및 디자인 가이드

### A. 전체 스타일
- **Color:** White 배경 베이스 + Light Gray 테두리 (Google 검색바 스타일).
- **Typography:** 깔끔하고 가독성 높은 '나눔 바른 고딕'.

### B. 애니메이션 & 인터랙션
- **메인 피드 로딩:**
    - 말풍선들이 화면에 나타날 때 **Fade-in**과 함께 아주 살짝 아래에서 위로 올라오는(**Slide-up**) 효과를 주어 정원이 피어나는 듯한 느낌 연출.
    - 한 번에 뜨지 않고 순차적으로(Staggered) 로딩되면 더 좋음.
- **상세 보기 팝업:**
    - 말풍선 클릭 시, 상세 내용 모달창이 화면 하단에서 부드럽게 위로 올라오는 **Slide-up** 애니메이션 적용.
    - 닫을 때는 역순으로 내려가거나 Fade-out.

### C. 레이아웃
- **메인 피드:** **Masonry Layout** (벽돌 쌓기 형태). 말풍선의 높이가 내용 길이에 따라 다를 수 있음을 고려.
- **말풍선 컴포넌트:**
    - 둥근 네모 형태 + 우측 하단 화살표 꼬리 이미지.
    - 테두리는 연한 회색 실선.

## 4. 기능 명세 (Specifications)

### A. 회원 관리 (Supabase Auth)
- **정보:** 이메일(ID), 비밀번호(8자 이상), 별명(최대 6자, 한글/영문/숫자).
- **제약 사항:**
    - **별명 중복 불가 (Unique).**
    - 별명 변경 시 `Diaries` 테이블의 작성자 정보와 동기화(또는 Join 쿼리 활용)하여 작성된 모든 글의 별명이 즉시 변경되어야 함.

### B. 메인 화면 (Public Garden)
- **피드 노출:**
    - 작성된 감사 일기를 Masonry Grid로 표시.
    - 초기 로딩 시 최신순 21개 제한.
- **말풍선 내용:**
    - 상단: 작성자 별명, 작성일.
    - 본문: 감사 내용 표시. 각 항목이 30자를 넘어가면 `...`으로 말줄임 처리.
- **상세 보기:** 클릭 시 팝업으로 전체 내용(최대 3문장) 표시.
- **좋아요 (🌳):**
    - **로그인한 사용자만** 클릭 가능.
    - 말풍선 1개당 1번만 가능 (Toggle 방식).
    - 비로그인 사용자가 클릭 시 로그인 유도.
- **비로그인 사용자 제약:**
    - 스크롤 하단 도달 시 "회원으로 가입하시고 함께 감사를 공유하세요." 문구 표시 및 무한 스크롤 차단.

### C. 나의 일기 (My Log)
- **작성 기능:**
    - **Server Time(DB 시간)** 기준 **당일** 일기만 작성 가능.
    - 최대 3개의 Input Box 제공 (각 100자 제한).
    - 저장 시 각 항목을 구분자(예: `*` 혹은 JSON 배열)로 처리하여 DB 저장.
- **통계 (Stats):**
    - 상단: 총 작성 일기 수.
    - **Contribution Graph:** GitHub 스타일의 잔디 심기 UI (작성한 날짜에 초록색 블록).
- **기록 조회:**
    - 날짜 필터링 제공.
    - 기본 뷰: 최신순 9개 그리드 표시 + 페이지네이션.

## 5. 데이터베이스 스키마 (PostgreSQL)

> *Supabase에서 아래 구조로 Table을 생성하세요.*

1.  **`profiles` (Users 확장)**
    - `id`: uuid (Primary Key, references auth.users)
    - `email`: text
    - `nickname`: text (Unique constraint 필수)
    - `created_at`: timestamptz

2.  **`diaries`**
    - `id`: uuid (Primary Key)
    - `user_id`: uuid (references profiles.id)
    - `content`: text (또는 text[]) - 감사 내용
    - `created_at`: timestamptz (Default: now())
    - `like_count`: integer (Default: 0) - *성능을 위해 역정규화하거나 count 쿼리 사용 선택*

3.  **`likes`**
    - `id`: bigint (Primary Key)
    - `user_id`: uuid (references profiles.id)
    - `diary_id`: uuid (references diaries.id)
    - **Constraint:** `Unique Key (user_id, diary_id)` - 중복 좋아요 방지

## 6. 개발 단계 (Milestones)

1.  **Phase 1: Setup & Auth**
    - Supabase 프로젝트 생성 및 Vercel 배포 연동.
    - DB Schema 설정 (RLS 정책 포함).
    - 회원가입(별명 중복 체크), 로그인 UI/Logic 구현.
2.  **Phase 2: My Diary (Core)**
    - 나의 일기 작성 (서버 시간 체크, 3줄 제한).
    - 잔디(Contribution) UI 및 통계 구현.
    - Mock Data로 테스트.
3.  **Phase 3: Public Garden (UI/Interaction)**
    - Main Page Masonry Layout 구현.
    - 말풍선 컴포넌트 & 애니메이션(Fade-in, Slide-up) 적용.
    - 좋아요 기능 및 비로그인 제약 구현.

---

**[클로드 코드 전달용 명령어]**
"위 PRD 문서를 바탕으로 프로젝트를 시작해줘.
1. 먼저 React + Vite + TypeScript + Tailwind CSS 환경을 세팅하고,
2. Supabase와 연동하여 회원가입/로그인 기능부터 구현해 줘."